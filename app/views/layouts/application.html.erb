<!DOCTYPE html>
<html>
  <head>
    <title>ShareYourDog</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/flatpickr/dist/flatpickr.min.css">
  </head>

  <body>
    <nav class="navbar navbar-expand-lg mb-2">
      <div class="container-fluid">
        <%= link_to 'ShareYourDog', root_path, class: 'navbar-brand' %>
        <div class="collapse navbar-collapse d-flex justify-content-end" id="navbarSupportedContent">
            <%# <ul class="navbar-nav me-auto mb-2 mb-lg-0">
              <li class="nav-item">
                <a class="nav-link active mt-2" aria-current="page" href="/dogs">All Dogs</a>
              </li>
                <ul class="dropdown-menu"> %>
                <%# <% Dog.all.each do |dog| %>
                  <%# <li><%= link_to "#{dog.name}", dog_path(dog), class:"dropdown-item" %></li>
                <%# <% end %>
              <%# </ul>
          </ul> %>

          <% if !user_signed_in? %>
          <%= link_to "Log in", user_session_path, class: "btn-account"%>
          <%= link_to "All dogs", dogs_path %>
          <% else %>

          <%= link_to "All Dogs", dogs_path %>
          <%= link_to "My Account", bookings_path, class: "btn-account "%>
          <%= link_to "Log out", destroy_user_session_path, data: {turbo_method: :delete}, class: "btn-account"%>
          <% end %>
        </div>
      </div>
    </nav>

  <%= render 'pages/flashes' %>

  <div class="container">
    <%= yield %>
  </div>
  </body>
</html>
